<form method="post" action="{{ path('reservation_delete', {'id': reservation[0].id}) }}" onsubmit="return confirm('Silmek İstediğinize emin misiniz?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation[0].id) }}">
    <button class="btn">Sil</button>
</form>
